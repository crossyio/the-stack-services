[Unit]
Description=Register with vulcand for crossyio-contributor-service-green
BindsTo=crossyio-contributor-service-green@%i.service
After=crossyio-contributor-service-green@%i.service

[Service]
EnvironmentFile=/etc/environment
RemainAfterExit=yes
ExecStart=/bin/sh -c "/usr/bin/echo 'Adding backend, server, and frontend to vulcand' && \
  /usr/bin/docker run --rm octoblu/vctl backend upsert \
    --id crossyio-contributor-service-green \
    --vulcan http://$(ifconfig docker0 | grep 'inet ' | awk '{print $2}'):8182 && \
  /usr/bin/docker run --rm octoblu/vctl server upsert \
    --id crossyio-contributor-service-green-%i \
    --backend crossyio-contributor-service-green \
    --url http://${COREOS_PRIVATE_IPV4}:$(/usr/bin/etcdctl get /crossyio/contributor-service/green/port) \
    --vulcan http://$(ifconfig docker0 | grep 'inet ' | awk '{print $2}'):8182 \
"

ExecStop=/bin/sh -c "/usr/bin/echo 'Removing server from vulcand' && \
  /usr/bin/docker run --rm octoblu/vctl server rm --id crossyio-contributor-service-green-%i --vulcan http://$(ifconfig docker0 | grep 'inet ' | awk '{print $2}'):8182"

[X-Fleet]
X-ConditionMachineOf=crossyio-contributor-service-green@%i.service
